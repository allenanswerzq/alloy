# alloy-transport-plain-http

Plain JSON-over-HTTP transport implementation.

This crate is for HTTP APIs that accept a JSON request body and return a JSON response body without
using JSON-RPC 2.0 envelopes. Use `alloy-transport-http` for Ethereum JSON-RPC endpoints.